from django.urls import re_path
from . import views

urlpatterns = [
    
    #re_path('<str:user>/all_messages', views.all_messages),
    #re_path('<str:user>/unread_messages', views.unread_messages),
    #re_path('<str:user>/messages_details/<str:pk>', views.message_details),
    #re_path('<str:user>/delete_messages', views.delete_message),
    
    
]